# Prompt Engineering Basics

Few-shot prompting gives the model a handful of worked examples before the real task.
It usually improves accuracy on structured tasks, though the gain depends on example quality.

Chain-of-thought prompting asks the model to reason step by step before answering.
It helps most on multi-step problems and tends to matter less on simple lookups.
